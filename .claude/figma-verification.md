# Протокол проверки по Figma макетам

**ОБЯЗАТЕЛЬНО запускать после внесения изменений в секции!**

Этот протокол добавлен после feedback от тимлида (январь 2026).

---

## Новые проверки (критичные)

### 1. Typography Verification

**Проверяемые параметры:**

- [ ] **Font-body**: `--font-body: 16px` (не 22px)
- [ ] **Font-weight-medium**: `--font-weight-medium: 200` (не 500)
- [ ] Измерить font-size основного текста в Figma (должно быть 16px на desktop)
- [ ] Проверить читаемость на мобильных (font-weight: 200 может быть слишком тонким)

**Где проверять:**
- `assets/css/base-styles.css` строки 31-32
- Классы `.text-medium-small` используют `--font-body`

**Критичность:** 🔴 Высокая (влияет на весь текст сайта)

---

### 2. Button States (Promotional/Features CTA)

**Проверяемые параметры:**

- [ ] **Border присутствует**: `border: 2px solid #000` на кнопках со стрелками
- [ ] **Border-radius**: 30px
- [ ] **Padding**: 15px 30px
- [ ] **Hover анимация**: `transform: translateX(5px)` при наведении
- [ ] **Стрелка анимируется**: `span` внутри кнопки двигается вместе с кнопкой
- [ ] Сравнить с Figma hover состоянием (если есть в макете)

**Где проверять:**
- `sections/promotional/promotional.css` (строки 22-45)
- `sections/features/features.css` (строки 12-35)

**Критичность:** 🟡 Средняя (визуальный стиль)

---

### 3. Services Cascade Direction

**Проверяемые параметры:**

- [ ] **Порядок карточек**: 1-я самая низкая (margin-top: 86px), 4-я самая высокая (0px)
- [ ] Визуально сравнить с Figma макетом (восходящий порядок)
- [ ] Проверить что на мобильных каскад сбрасывается (margin-top: 0)
- [ ] На планшетах (< 991px) каскад также сброшен

**Где проверять:**
- `sections/services/services.css` (строки 12-26)
- Desktop: должен быть каскад 86px → 56px → 26px → 0px
- Mobile (< 991px): все карточки margin-top: 0

**Критичность:** 🟡 Средняя (визуальный порядок)

---

### 4. Background Consistency (все страницы)

**Проверяемые параметры:**

- [ ] **Index.html**: Чередование Primary (#F5EDE0) и Secondary (#EEE3D0)
- [ ] **Quiz страницы**: Все секции используют Primary (#F5EDE0)
- [ ] **Footer на quiz-form**: #F5EDE0 (не #FFFFFF)
- [ ] **Footer на thank-you**: #F5EDE0 (явно указан)
- [ ] **Legal-footer**: консистентный с основной страницей

**Где проверять:**
- `landings/<project>/config.json` - все backgroundColor
- Сравнить с Figma node IDs для каждой страницы

**Критичность:** 🔴 Высокая (дизайн консистентность)

---

### 5. Process Section Layout

**Проверяемые параметры:**

- [ ] **Flex-direction: row** (иконка и заголовок в одной строке)
- [ ] **Иконка размер**: 32x32px (не 76x75px)
- [ ] **Иконка тип**: SVG галочка (не картинка)
- [ ] **Фиксация элементов**: `flex-shrink: 0` на иконке, `flex: 1` на заголовке
- [ ] При длинном тексте заголовок переносится, иконка остается на месте
- [ ] На мобильных (< 768px) layout остается row

**Где проверять:**
- `sections/process/process.css` (строки 11-39)
- `sections/process/process.html` (строки 68-81) - SVG галочка

**Критичность:** 🟡 Средняя (layout структура)

---

### 6. Visual Consistency

**Общие проверки:**

- [ ] Все hover эффекты соответствуют макету
- [ ] Spacing между элементами совпадает с Figma
- [ ] Border-radius на кнопках соответствует дизайну (30px для CTA, 15px для карточек)
- [ ] Цвета совпадают с Design Tokens
- [ ] Шрифты соответствуют (Inter, 16px body)

**Критичность:** 🟢 Низкая (детали)

---

## Design Tokens (обязательные)

```css
/* Из Figma макета */
--brand-primary: #F5EDE0;     /* Primary background */
--brand-secondary: #EEE3D0;   /* Secondary background */
--brand-accent: #E2C08D;      /* Accent color (buttons, highlights) */
--brand-text: #3D3D3D;        /* Main text color */
--font-body: 16px;            /* Body text size */
--font-weight-medium: 200;    /* Medium weight (Extra Light) */
--font-weight-bold: 700;      /* Bold weight */
```

---

## Процесс проверки

### Шаг 1: Проверить CSS переменные

```bash
# Проверить base-styles.css
grep -n "font-body:" assets/css/base-styles.css
# Ожидается: --font-body: 16px;

grep -n "font-weight-medium:" assets/css/base-styles.css
# Ожидается: --font-weight-medium: 200;
```

### Шаг 2: Проверить Services cascade

```bash
grep -n "nth-child" sections/services/services.css
# Ожидается:
# nth-child(1) margin-top: 86px
# nth-child(2) margin-top: 56px
# nth-child(3) margin-top: 26px
# nth-child(4) margin-top: 0
```

### Шаг 3: Проверить Process layout

```bash
grep -n "flex-direction:" sections/process/process.css
# Ожидается: flex-direction: row;
```

### Шаг 4: Визуальная проверка

Открыть скриншоты:
- Desktop (1440px): Все элементы видны, каскад работает
- Tablet (768px): Каскад сброшен, buttons адаптированы
- Mobile (375px): Typography читаема, borders видны

---

## Частые ошибки

### ❌ Ошибка 1: Font-weight слишком жирный

**Проблема:** `--font-weight-medium: 500` вместо `200`

**Решение:**
```css
/* В assets/css/base-styles.css */
--font-weight-medium: 200;
```

**Признаки:** Текст выглядит жирнее чем в Figma

---

### ❌ Ошибка 2: Кнопки без border

**Проблема:** У `.promotional-cta a` нет `border: 2px solid #000`

**Решение:**
```css
/* В sections/promotional/promotional.css */
.promotional-cta a {
  border: 2px solid #000000;
  border-radius: 30px;
  padding: 15px 30px;
}
```

**Признаки:** Кнопки со стрелками не имеют рамки

---

### ❌ Ошибка 3: Services каскад в неправильном направлении

**Проблема:** 1-я карточка выше, 4-я ниже (нисходящий)

**Решение:**
```css
/* В sections/services/services.css */
.services .row > div:nth-child(1) .service-card { margin-top: 86px; }
.services .row > div:nth-child(2) .service-card { margin-top: 56px; }
.services .row > div:nth-child(3) .service-card { margin-top: 26px; }
.services .row > div:nth-child(4) .service-card { margin-top: 0; }
```

**Признаки:** Каскад идет вниз вместо вверх

---

### ❌ Ошибка 4: Footer backgrounds несоответствуют

**Проблема:** Footer на quiz-form.html имеет #FFFFFF вместо #F5EDE0

**Решение:**
```json
/* В landings/<project>/config.json */
{
  "type": "footer",
  "content": {
    "backgroundColor": "#F5EDE0"
  }
}
```

**Признаки:** Footer белый вместо бежевого

---

### ❌ Ошибка 5: Process иконка вертикально

**Проблема:** `flex-direction: column` в `.process-step-header`

**Решение:**
```css
/* В sections/process/process.css */
.process-step-header {
  display: flex;
  flex-direction: row;
  align-items: flex-start;
}
```

**Признаки:** Иконка над заголовком вместо слева

---

## Чеклист финальной проверки

Перед сдачей лендинга проверить:

### Typography
- [ ] Font-body = 16px
- [ ] Font-weight-medium = 200
- [ ] Текст читаем на всех устройствах

### Buttons
- [ ] Border 2px solid black на CTA со стрелками
- [ ] Hover анимация работает (translateX + background)
- [ ] Border-radius = 30px

### Services
- [ ] Каскад восходящий (1-я низко, 4-я высоко)
- [ ] На мобильных каскад сброшен

### Backgrounds
- [ ] Все backgrounds соответствуют Figma (#F5EDE0 / #EEE3D0)
- [ ] Footer на quiz страницах #F5EDE0

### Process
- [ ] Иконка и заголовок в одной строке
- [ ] SVG галочка 32x32px
- [ ] При длинном тексте layout не ломается

### Visual
- [ ] Все spacing совпадают с Figma
- [ ] Цвета соответствуют Design Tokens
- [ ] Hover states работают

---

## Контакты

**Если что-то не совпадает с Figma:**

1. Открыть Figma макет
2. Измерить spacing/sizes/colors
3. Сравнить с CSS
4. Исправить расхождения
5. Пересобрать лендинг: `node builder/builder.js <project>`
6. Повторить проверку

**Важно:** Все изменения должны быть задокументированы в git commits!

---

## История изменений

- **2026-01-10**: Добавлен протокол после feedback тимлида
  - Typography: font-body 22px→16px, font-weight-medium 500→200
  - Buttons: добавлен border и hover анимация
  - Services: инвертирован каскад на восходящий
  - Process: изменен layout на row, добавлена SVG галочка
  - Backgrounds: исправлены footer на quiz страницах
